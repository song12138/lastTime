<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="pro.dao.ShiroDao">


    <select id="findByUsername" resultType="SysUser" parameterType="java.lang.String">
        SELECT *
        FROM
        sys_user
        <where>
            1=2
            <if test="username != null and username != ''">
                OR username=#{username}
            </if>
        </where>
    </select>


    <select id="findRoles" parameterType="java.lang.String" resultType="SysRole">
        SELECT *
        FROM
       sys_role a
       LEFT JOIN sys_user_role b on a.id=b.role_id
        LEFT JOIN sys_user c ON b.user_id=c.id
       <where>
            1=2
            <if test="username != null and username != ''">
                OR c.username=#{username}
            </if>
       </where>

    </select>


    <select id="findPermissions" resultType="SysResource" parameterType="java.lang.String">
        SELECT *
        FROM
        sys_resource a
        LEFT JOIN sys_role_resource b on a.id=b.resource_id
        LEFT JOIN sys_role c ON b.role_id =c.id
        LEFT JOIN sys_user_role d ON c.id=d.role_id
        LEFT JOIN sys_user e ON d.user_id=e.id
        <where>
            1=2
            <if test="username != null and username != ''">
                OR e.username=#{username}
            </if>
        </where>
    </select>


    <insert id="addUser" parameterType="SysUser">
        replace INTO sys_user (
             id,
            username,
            realname,
            password,
            salt,
            locked
        ) VALUES (
          #{id},
          #{username},
          #{realname},
          #{password},
          #{salt},
          #{locked}
        )
    </insert>

</mapper>